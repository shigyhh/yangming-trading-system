.mind-shell {
  position: relative;
  min-height: 100vh;
  overflow: hidden;
  padding: calc(48rpx + env(safe-area-inset-top)) 48rpx calc(170rpx + env(safe-area-inset-bottom));
  color: #f7efdf;
  background:
    radial-gradient(circle at 72% 6%, rgba(211, 168, 79, 0.14), transparent 28%),
    linear-gradient(180deg, #0b1118 0%, #070d11 45%, #030606 100%);
  font-family: "ZX-Harmony", "HarmonyOS Sans SC", "HarmonyOS Sans", -apple-system, BlinkMacSystemFont, "PingFang SC", sans-serif;
}

.mind-bg {
  position: fixed;
  inset: 0;
  pointer-events: none;
}

.mist {
  opacity: 0.7;
  background:
    radial-gradient(ellipse at 50% 20%, rgba(255, 246, 226, 0.06), transparent 42%),
    linear-gradient(110deg, transparent 12%, rgba(255, 255, 255, 0.026) 42%, transparent 72%);
}

.grain {
  opacity: 0.09;
  background:
    linear-gradient(90deg, rgba(241, 210, 125, 0.05) 1rpx, transparent 1rpx),
    linear-gradient(180deg, rgba(241, 210, 125, 0.035) 1rpx, transparent 1rpx);
  background-size: 92rpx 92rpx;
  animation: grainDrift 24s linear infinite;
}

.halo {
  top: -20%;
  left: 18%;
  right: auto;
  bottom: auto;
  width: 520rpx;
  height: 520rpx;
  border-radius: 50%;
  opacity: 0.18;
  background: radial-gradient(circle, rgba(241, 210, 125, 0.22), transparent 64%);
  animation: breath 7s ease-in-out infinite;
}

.mind-content {
  position: relative;
  z-index: 2;
  animation: riseIn 0.78s cubic-bezier(0.19, 1, 0.22, 1) both;
}

.mind-head {
  display: flex;
  align-items: center;
  gap: 22rpx;
}

.seal {
  width: 82rpx;
  height: 82rpx;
  display: grid;
  place-items: center;
  border-radius: 24rpx;
  color: #f6d886;
  border: 1rpx solid rgba(241, 210, 125, 0.36);
  background: rgba(255, 255, 255, 0.045);
  font-family: "ZX-LXGW", "LXGW WenKai", "霞鹜文楷", "Songti SC", serif;
  font-size: 42rpx;
  font-weight: 900;
}

.mind-head text {
  color: #fff7e7;
  font-size: 30rpx;
  font-weight: 900;
}

.mind-head view view {
  margin-top: 8rpx;
  color: rgba(241, 210, 125, 0.48);
  font-size: 20rpx;
  font-weight: 800;
  letter-spacing: 0.16em;
}

.hero {
  margin-top: 78rpx;
}

.hero text {
  color: rgba(241, 210, 125, 0.54);
  font-size: 22rpx;
  font-weight: 900;
  letter-spacing: 0.28em;
}

.hero view {
  margin-top: 20rpx;
  color: #fff7e7;
  font-family: "ZX-LXGW", "LXGW WenKai", "霞鹜文楷", "Songti SC", serif;
  font-size: 58rpx;
  font-weight: 900;
  line-height: 1.18;
}

.mind-instrument {
  position: relative;
  width: 100%;
  height: 650rpx;
  margin-top: 56rpx;
  overflow: hidden;
  border-radius: 44rpx;
  background:
    radial-gradient(circle at 50% 52%, rgba(241, 210, 125, 0.11), transparent 30%),
    linear-gradient(145deg, rgba(255, 255, 255, 0.048), rgba(255, 255, 255, 0.014)),
    rgba(8, 13, 12, 0.46);
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.07),
    0 34rpx 90rpx rgba(0, 0, 0, 0.18);
  backdrop-filter: blur(28rpx);
}

.instrument-line {
  position: absolute;
  left: 36rpx;
  right: 36rpx;
  top: 0;
  height: 1rpx;
  background: linear-gradient(90deg, transparent, rgba(241, 210, 125, 0.68), transparent);
  animation: threadFlow 5s ease-in-out infinite;
}

.instrument-status {
  position: absolute;
  left: 34rpx;
  top: 30rpx;
  right: 34rpx;
  z-index: 6;
  display: flex;
  justify-content: space-between;
  gap: 20rpx;
  color: rgba(247, 239, 223, 0.48);
}

.instrument-status text {
  color: rgba(241, 210, 125, 0.58);
  font-size: 20rpx;
  font-weight: 900;
  letter-spacing: 0.22em;
}

.instrument-status view {
  font-size: 21rpx;
  font-weight: 800;
}

.ring {
  position: absolute;
  left: 50%;
  top: 52%;
  border-radius: 50%;
  border: 1rpx solid rgba(241, 210, 125, 0.12);
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.ring-a {
  width: 500rpx;
  height: 500rpx;
  animation: slowSpin 28s linear infinite;
}

.ring-b {
  width: 380rpx;
  height: 380rpx;
  border-style: dashed;
  opacity: 0.8;
  animation: slowSpinReverse 36s linear infinite;
}

.ring-c {
  width: 240rpx;
  height: 240rpx;
  opacity: 0.6;
  animation: breath 6.8s ease-in-out infinite;
}

.instrument-core {
  position: absolute;
  left: 50%;
  top: 52%;
  z-index: 5;
  width: 250rpx;
  height: 250rpx;
  padding-top: 34rpx;
  display: grid;
  justify-items: center;
  align-content: start;
  border-radius: 50%;
  color: #f6d886;
  border: 1rpx solid rgba(241, 210, 125, 0.4);
  background:
    radial-gradient(circle at 50% 40%, rgba(241, 210, 125, 0.18), transparent 42%),
    rgba(255, 255, 255, 0.022);
  box-shadow:
    inset 0 0 46rpx rgba(241, 210, 125, 0.08),
    0 0 82rpx rgba(241, 210, 125, 0.08);
  transform: translate(-50%, -50%);
}

.core-glow {
  position: absolute;
  inset: -40rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(241, 210, 125, 0.12), transparent 68%);
  animation: breath 5.8s ease-in-out infinite;
}

.core-glyph {
  position: relative;
  z-index: 1;
  font-family: "ZX-LXGW", "LXGW WenKai", "霞鹜文楷", "Songti SC", serif;
  font-size: 70rpx;
  font-weight: 900;
}

.core-name {
  position: relative;
  z-index: 1;
  margin-top: 2rpx;
  color: #fff7e7;
  font-size: 24rpx;
  font-weight: 900;
}

.core-subtitle {
  position: relative;
  z-index: 1;
  margin-top: 6rpx;
  color: rgba(247, 239, 223, 0.5);
  font-size: 20rpx;
}

.wave {
  position: relative;
  z-index: 1;
  height: 54rpx;
  margin-top: 14rpx;
  display: flex;
  align-items: center;
  gap: 6rpx;
}

.wave view {
  width: 7rpx;
  border-radius: 999rpx;
  background: linear-gradient(180deg, rgba(246, 216, 134, 0.96), rgba(84, 176, 158, 0.64));
  animation: waveBreath 1.8s ease-in-out infinite;
}

.wave view:nth-child(2n) {
  animation-delay: 0.24s;
}

.wave view:nth-child(3n) {
  animation-delay: 0.42s;
}

.orbit-node {
  position: absolute;
  z-index: 7;
  width: 104rpx;
  height: 104rpx;
  display: grid;
  place-items: center;
  align-content: center;
  border-radius: 32rpx;
  color: rgba(247, 239, 223, 0.52);
  border: 1rpx solid rgba(255, 255, 255, 0.07);
  background: rgba(255, 255, 255, 0.032);
  backdrop-filter: blur(16rpx);
}

.orbit-node text {
  color: currentColor;
  font-family: "ZX-LXGW", "LXGW WenKai", "霞鹜文楷", "Songti SC", serif;
  font-size: 38rpx;
  font-weight: 900;
}

.orbit-node view {
  margin-top: 4rpx;
  font-size: 18rpx;
  font-weight: 800;
}

.orbit-node.active {
  color: #f6d886;
  border-color: rgba(241, 210, 125, 0.36);
  background: rgba(201, 157, 76, 0.1);
  box-shadow:
    0 0 34rpx rgba(201, 157, 76, 0.12),
    inset 0 0 18rpx rgba(241, 210, 125, 0.08);
}

.node-0 { left: 50%; top: 92rpx; transform: translateX(-50%); }
.node-1 { right: 72rpx; top: 156rpx; }
.node-2 { right: 48rpx; top: 50%; transform: translateY(-50%); }
.node-3 { right: 72rpx; bottom: 74rpx; }
.node-4 { left: 50%; bottom: 40rpx; transform: translateX(-50%); }
.node-5 { left: 72rpx; bottom: 74rpx; }
.node-6 { left: 48rpx; top: 50%; transform: translateY(-50%); }
.node-7 { left: 72rpx; top: 156rpx; }

.breath-panel {
  margin-top: 30rpx;
  padding: 26rpx;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 22rpx;
  align-items: center;
  border-radius: 30rpx;
  background: rgba(255, 255, 255, 0.028);
  border: 1rpx solid rgba(241, 210, 125, 0.12);
}

.breath-panel > view:first-child text {
  display: block;
  color: rgba(241, 210, 125, 0.62);
  font-size: 22rpx;
  font-weight: 900;
  letter-spacing: 0.18em;
}

.breath-panel > view:first-child view {
  margin-top: 10rpx;
  color: #fff7e7;
  font-size: 30rpx;
  font-family: "ZX-LXGW", "LXGW WenKai", "霞鹜文楷", "Songti SC", serif;
  font-weight: 900;
}

.breath-dots {
  display: flex;
  gap: 10rpx;
}

.breath-dots i {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.12);
}

.breath-dots i.active {
  background: #f6d886;
  box-shadow: 0 0 24rpx rgba(241, 210, 125, 0.32);
}

.breath-panel button {
  grid-column: 1 / -1;
  height: 78rpx;
  display: grid;
  place-items: center;
  border-radius: 999rpx;
  color: #f6d886;
  border: 1rpx solid rgba(241, 210, 125, 0.24);
  background: rgba(201, 157, 76, 0.06);
  font-size: 26rpx;
  font-weight: 900;
}

.ritual-scroll {
  position: relative;
  margin-top: 46rpx;
  padding: 42rpx 30rpx 38rpx;
  overflow: hidden;
  border-radius: 38rpx;
  background:
    linear-gradient(145deg, rgba(255, 255, 255, 0.052), rgba(255, 255, 255, 0.016)),
    rgba(8, 13, 12, 0.58);
  box-shadow:
    inset 0 1rpx 0 rgba(255, 255, 255, 0.07),
    0 30rpx 80rpx rgba(0, 0, 0, 0.18);
  backdrop-filter: blur(28rpx);
}

.scroll-line {
  position: absolute;
  left: 30rpx;
  right: 30rpx;
  top: 0;
  height: 1rpx;
  background: linear-gradient(90deg, transparent, rgba(241, 210, 125, 0.68), transparent);
  animation: threadFlow 5s ease-in-out infinite;
}

.ritual-glyph {
  display: none;
}

.ritual-title {
  color: #fff7e7;
  font-family: "ZX-LXGW", "LXGW WenKai", "霞鹜文楷", "Songti SC", serif;
  font-size: 46rpx;
  font-weight: 900;
}

.ritual-subtitle {
  margin-top: 10rpx;
  color: rgba(247, 239, 223, 0.5);
  font-size: 24rpx;
}

.ritual-list {
  margin-top: 36rpx;
  display: grid;
  gap: 24rpx;
}

.ritual-item {
  padding-bottom: 22rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.065);
}

.ritual-item:last-child {
  border-bottom: 0;
  padding-bottom: 0;
}

.ritual-item text {
  display: block;
  color: rgba(241, 210, 125, 0.6);
  font-size: 22rpx;
  font-weight: 900;
  letter-spacing: 0.18em;
}

.ritual-item view {
  margin-top: 12rpx;
  color: rgba(247, 239, 223, 0.72);
  font-size: 27rpx;
  line-height: 1.7;
}

.ritual-item.main view {
  color: #fff7e7;
  font-size: 32rpx;
  font-weight: 900;
}

.loop-strip {
  margin-top: 34rpx;
  display: flex;
  gap: 12rpx;
  overflow: hidden;
}

.loop-strip view {
  flex: 1;
  min-width: 0;
  display: grid;
  place-items: center;
  height: 54rpx;
  border-radius: 999rpx;
  color: rgba(247, 239, 223, 0.36);
  border: 1rpx solid rgba(255, 255, 255, 0.055);
  font-size: 20rpx;
  font-weight: 800;
}

.loop-strip .current {
  color: #11130d;
  background: linear-gradient(180deg, #f6d886, #c99d4c);
}

.actions {
  margin-top: 38rpx;
  display: grid;
  grid-template-columns: 0.82fr 1.18fr;
  gap: 16rpx;
}

.secondary,
.primary {
  height: 88rpx;
  display: grid;
  place-items: center;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 900;
}

.secondary {
  color: #f6d886;
  border: 1rpx solid rgba(241, 210, 125, 0.28);
  background: rgba(201, 157, 76, 0.06);
}

.primary {
  color: #11130d;
  background: linear-gradient(105deg, rgba(255, 245, 205, 0.96), rgba(227, 181, 82, 0.96), rgba(255, 238, 181, 0.96));
  background-size: 200% 100%;
  box-shadow: 0 24rpx 54rpx rgba(201, 157, 76, 0.22);
  animation: buttonGoldFlow 5.4s ease-in-out infinite;
}

.review-link {
  margin-top: 28rpx;
  color: rgba(247, 239, 223, 0.42);
  font-size: 23rpx;
  text-align: center;
}

.mind-ripple {
  position: fixed;
  z-index: 8;
  width: 28rpx;
  height: 28rpx;
  margin-left: -14rpx;
  margin-top: -14rpx;
  border-radius: 50%;
  border: 1rpx solid rgba(241, 210, 125, 0.42);
  box-shadow: 0 0 36rpx rgba(241, 210, 125, 0.1);
  animation: ripple 1.08s ease-out forwards;
  pointer-events: none;
}

@keyframes riseIn {
  from {
    opacity: 0;
    transform: translateY(32rpx) scale(0.985);
    filter: blur(8rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes grainDrift {
  from { background-position: 0 0; }
  to { background-position: 92rpx 184rpx; }
}

@keyframes breath {
  0%, 100% {
    opacity: 0.14;
    transform: scale(0.96);
  }
  50% {
    opacity: 0.28;
    transform: scale(1.06);
  }
}

@keyframes threadFlow {
  0%, 100% {
    opacity: 0.28;
    transform: scaleX(0.76);
  }
  50% {
    opacity: 0.9;
    transform: scaleX(1);
  }
}

@keyframes buttonGoldFlow {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes ripple {
  from {
    opacity: 0.46;
    transform: scale(0.2);
  }
  to {
    opacity: 0;
    transform: scale(15);
  }
}

@media (max-width: 340px) {
  .mind-shell {
    padding-left: 42rpx;
    padding-right: 42rpx;
  }

  .mind-instrument {
    height: 590rpx;
  }

  .instrument-core {
    width: 220rpx;
    height: 220rpx;
  }

  .ring-a {
    width: 430rpx;
    height: 430rpx;
  }

  .ring-b {
    width: 330rpx;
    height: 330rpx;
  }

  .orbit-node {
    width: 90rpx;
    height: 90rpx;
    border-radius: 26rpx;
  }

  .orbit-node text {
    font-size: 34rpx;
  }

  .orbit-node view {
    font-size: 17rpx;
  }

  .node-1,
  .node-3 {
    right: 42rpx;
  }

  .node-5,
  .node-7 {
    left: 42rpx;
  }

  .node-2 {
    right: 28rpx;
  }

  .node-6 {
    left: 28rpx;
  }

  .actions {
    grid-template-columns: 1fr;
  }
}
